/**
 * @param {string} s
 * @param {number} k
 * @return {string}
 */
var truncateSentence = function(s, k) {
    const half = s.split(' ')
    let arr = []
    for(i=0; i<k; i++) {
        arr.push(half[i])
    }
    return arr.join(' ')
};
